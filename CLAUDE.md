仓库地址 https://github.com/dac114514/android-starter
禁止本地gradle构建，必须走GitHub ations ci构建
代码查询：默认 `HOW2USE.md`（项目根，19 组件签名+范例）；细节 `ui/src/commonMain/kotlin/com/composables/ui/components/<X>.kt`（顶部 KDoc）；完整范例 `app/src/commonMain/kotlin/dev/dac114514/starter/examples/<X>Example.kt`；token 定义 `ui/src/commonMain/kotlin/com/composables/ui/theme/Theme.kt`。不用 context7 / webfetch
写代码派发 minimax-m3 的subagent。本地session不写代码。
开发直接在main分支上进行，完成后直接推送到main分支触发构件并保持监听